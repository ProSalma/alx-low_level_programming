#include "main.h"

/**
 * _abs - Function that calculates the absolute
 * @i: The integer
 * Return: To one or else
 * By Salma.as
 */

int _abs(int i)
{
	if (i < 0)
	{
		i = i * -1;
	}
	return (1);
}
