#include "main.h"

/**
 * main - Function that prints its name
 * @c:  The variable
 * @v: The Variable
 * Return: To 0
 * BySalma.as
 */

int main(int c, char **v)
{
	if (c > 0)
		printf("%s\n", *v);
	return (0);
}
